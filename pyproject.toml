[tool.uv.sources]
lineo-admin = { path = "packages/admin" }
lineo-pages = { path = "packages/pages" }

[tool.uv.workspace]
members = ["packages/*"]

[build-system]
requires = ["uv_build>=0.8.7,<0.9.0"]
build-backend = "uv_build"
